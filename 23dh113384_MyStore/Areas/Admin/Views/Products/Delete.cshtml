@model _23dh113384_MyStore.Models.Product

@{
    ViewBag.Title = "Delete";
    Layout = "~/Areas/Admin/Views/Shared/AdminLayout.cshtml";
}

<h2 class="bg-success">XÓA SẢN PHẨM  </h2>

<h4 class="bg-danger">Bạn thực sự muốn sản phẩm này?</h4>

@Html.Partial("PVProdInfo", Model)

<div class="form-group">
    <div class="col-md-offset-3 col-md-9">
        @Html.ActionLink("Xóa", "Delete", new { id = Model.ProductID }, new { @class = "btn btn-primary" })
        @Html.ActionLink("Quay lại danh mục ", "Index", null, new { @class = "btn btn-primary" })
    </div>
</div>
